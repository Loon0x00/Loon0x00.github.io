---
sidebar_position: 1
---

# 规则

规则用于判断请求应使用哪个策略，策略再决定最终节点。

## 匹配优先级

Loon 3.0.3 及以上版本使用以下匹配顺序：

1. 目标地址为域名时，先匹配域名规则。
2. 域名规则未命中时，再解析 DNS 并匹配 IP 规则。
3. 其他规则按配置顺序匹配，越靠前优先级越高。
4. 规则来源优先级为：**本地规则 > 插件规则 > 订阅规则**。
5. 所有规则均未命中时，使用 `FINAL` 指定的策略。
